@model WebApplication4.NewDiscModel
@{
    ViewBag.Title = "New discount";
}

<h2>New discount</h2>

@Html.ValidationSummary(true)

@using (Html.BeginForm())
{
    @Html.EditorForModel()
    <p>
        <button type="submit">Add</button>
    </p>
}

<p>
    <a href="@Url.Action("index", "discounts")">Back</a>
</p>
